services:
  - type: web
    name: cryptos
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    headers:
      - path: /assets/*
        name: Content-Type
        value: auto
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
